/**
 * Interface Segregation Principle (ISP)
 *  - Define fine-grained interfaces that are tailored to the needs
 *  of the consumers.
 *  - Avoid creating large interfaces unnecessary methods.
 *  - Split your interfaces into smaller, more focused ones, allowing
 *  clients to depends only on the interfaces they require.
 */

class InterfaceSegregationPrinciple {}
